# Loaded by install.sh and relayctl.sh. The master secret is deliberately not
# stored here; it lives in secrets/master-secret.
HAPPY_RELAY_IMAGE=happy-relay-server:__VERSION__-debian13-amd64
HAPPY_RELAY_BIND_ADDRESS=127.0.0.1
HAPPY_RELAY_PORT=3005
HAPPY_RELAY_PUBLIC_URL=http://localhost:3005
